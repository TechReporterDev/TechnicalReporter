// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "CustomReportData-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/view-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/view-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // QueryJoiningData
  //

  struct access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        id,
        i.m_key_value,
        i.m_key_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_key
    //
    t[0UL] = 0;

    // m_input_uuid
    //
    if (composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::grow (
          i.m_input_uuid_value, t + 1UL))
      grew = true;

    // m_output_uuid
    //
    if (composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::grow (
          i.m_output_uuid_value, t + 2UL))
      grew = true;

    // m_default_query_xml
    //
    if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::grow (
          i.m_default_query_xml_value, t + 3UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // m_key
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONG;
      b[n].is_unsigned = 0;
      b[n].buffer = &i.m_key_value;
      b[n].is_null = &i.m_key_null;
      n++;
    }

    // m_input_uuid
    //
    composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::bind (
      b + n, i.m_input_uuid_value, sk);
    n += 1UL;

    // m_output_uuid
    //
    composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::bind (
      b + n, i.m_output_uuid_value, sk);
    n += 1UL;

    // m_default_query_xml
    //
    composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::bind (
      b + n, i.m_default_query_xml_value, sk);
    n += 1UL;
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_key
    //
    if (sk == statement_insert)
    {
      ::TR::Core::Key const& v =
        o.m_key;

      bool is_null (false);
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_image (
        i.m_key_value, is_null, v);
      i.m_key_null = is_null;
    }

    // m_input_uuid
    //
    {
      ::TR::Core::ReportTypeUUID const& v =
        o.m_input_uuid;

      if (composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::init (
            i.m_input_uuid_value,
            v,
            sk))
        grew = true;
    }

    // m_output_uuid
    //
    {
      ::TR::Core::ReportTypeUUID const& v =
        o.m_output_uuid;

      if (composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::init (
            i.m_output_uuid_value,
            v,
            sk))
        grew = true;
    }

    // m_default_query_xml
    //
    {
      ::boost::optional< ::TR::Core::XmlQueryDocText > const& v =
        o.m_default_query_xml;

      if (wrapper_traits< ::boost::optional< ::TR::Core::XmlQueryDocText > >::get_null (v))
        composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::set_null (
          i.m_default_query_xml_value, sk);
      else
      {
        if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
              i.m_default_query_xml_value,
              wrapper_traits< ::boost::optional< ::TR::Core::XmlQueryDocText > >::get_ref (v),
              sk))
          grew = true;
      }
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_key
    //
    {
      ::TR::Core::Key& v =
        o.m_key;

      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        v,
        i.m_key_value,
        i.m_key_null);
    }

    // m_input_uuid
    //
    {
      ::TR::Core::ReportTypeUUID& v =
        o.m_input_uuid;

      composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::init (
        v,
        i.m_input_uuid_value,
        db);
    }

    // m_output_uuid
    //
    {
      ::TR::Core::ReportTypeUUID& v =
        o.m_output_uuid;

      composite_value_traits< ::TR::Core::ReportTypeUUID, id_mysql >::init (
        v,
        i.m_output_uuid_value,
        db);
    }

    // m_default_query_xml
    //
    {
      ::boost::optional< ::TR::Core::XmlQueryDocText >& v =
        o.m_default_query_xml;

      if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::get_null (
            i.m_default_query_xml_value))
        wrapper_traits< ::boost::optional< ::TR::Core::XmlQueryDocText > >::set_null (v);
      else
        composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
          wrapper_traits< ::boost::optional< ::TR::Core::XmlQueryDocText > >::set_ref (v),
          i.m_default_query_xml_value,
          db);
    }
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::persist_statement[] =
  "INSERT INTO `QueryJoiningData` "
  "(`key`, "
  "`input_uuid_value`, "
  "`output_uuid_value`, "
  "`default_query_xml_value`) "
  "VALUES "
  "(?, ?, ?, ?)";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::find_statement[] =
  "SELECT "
  "`QueryJoiningData`.`key`, "
  "`QueryJoiningData`.`input_uuid_value`, "
  "`QueryJoiningData`.`output_uuid_value`, "
  "`QueryJoiningData`.`default_query_xml_value` "
  "FROM `QueryJoiningData` "
  "WHERE `QueryJoiningData`.`key`=?";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::update_statement[] =
  "UPDATE `QueryJoiningData` "
  "SET "
  "`input_uuid_value`=?, "
  "`output_uuid_value`=?, "
  "`default_query_xml_value`=? "
  "WHERE `key`=?";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::erase_statement[] =
  "DELETE FROM `QueryJoiningData` "
  "WHERE `key`=?";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::query_statement[] =
  "SELECT "
  "`QueryJoiningData`.`key`, "
  "`QueryJoiningData`.`input_uuid_value`, "
  "`QueryJoiningData`.`output_uuid_value`, "
  "`QueryJoiningData`.`default_query_xml_value` "
  "FROM `QueryJoiningData`";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::erase_query_statement[] =
  "DELETE FROM `QueryJoiningData`";

  const char access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::table_name[] =
  "`QueryJoiningData`";

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.m_key_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.m_key = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.m_key);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::pointer_type
  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.m_key);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::object_type >
  access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::TR::Core::QueryJoiningData, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // SubjectQueryIdentity
  //

  bool access::composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_subject
    //
    if (composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::grow (
          i.m_subject_value, t + 0UL))
      grew = true;

    // m_query_joining_key
    //
    if (composite_value_traits< ::TR::Core::QueryJoiningKey, id_mysql >::grow (
          i.m_query_joining_key_value, t + 2UL))
      grew = true;

    return grew;
  }

  void access::composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // m_subject
    //
    composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::bind (
      b + n, i.m_subject_value, sk);
    n += 2UL;

    // m_query_joining_key
    //
    composite_value_traits< ::TR::Core::QueryJoiningKey, id_mysql >::bind (
      b + n, i.m_query_joining_key_value, sk);
    n += 1UL;
  }

  bool access::composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::
  init (image_type& i,
        const value_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_subject
    //
    {
      ::TR::Core::SubjectIdentity const& v =
        o.m_subject;

      composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::init (
        i.m_subject_value,
        v,
        sk);
    }

    // m_query_joining_key
    //
    {
      ::TR::Core::QueryJoiningKey const& v =
        o.m_query_joining_key;

      composite_value_traits< ::TR::Core::QueryJoiningKey, id_mysql >::init (
        i.m_query_joining_key_value,
        v,
        sk);
    }

    return grew;
  }

  void access::composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_subject
    //
    {
      ::TR::Core::SubjectIdentity& v =
        o.m_subject;

      composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::init (
        v,
        i.m_subject_value,
        db);
    }

    // m_query_joining_key
    //
    {
      ::TR::Core::QueryJoiningKey& v =
        o.m_query_joining_key;

      composite_value_traits< ::TR::Core::QueryJoiningKey, id_mysql >::init (
        v,
        i.m_query_joining_key_value,
        db);
    }
  }

  // SubjectQueryData
  //

  struct access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        id,
        i.m_identity_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_identity
    //
    if (composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::grow (
          i.m_identity_value, t + 0UL))
      grew = true;

    // m_query_xml
    //
    if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::grow (
          i.m_query_xml_value, t + 3UL))
      grew = true;

    // m_uuid
    //
    if (t[4UL])
    {
      i.m_uuid_value.capacity (i.m_uuid_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // m_identity
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
        b + n, i.m_identity_value, sk);
      n += 3UL;
    }

    // m_query_xml
    //
    composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::bind (
      b + n, i.m_query_xml_value, sk);
    n += 1UL;

    // m_uuid
    //
    b[n].buffer_type = MYSQL_TYPE_BLOB;
    b[n].buffer = i.m_uuid_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_uuid_value.capacity ());
    b[n].length = &i.m_uuid_size;
    b[n].is_null = &i.m_uuid_null;
    n++;
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    mysql::statement_kind sk (mysql::statement_select);
    composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_identity
    //
    if (sk == statement_insert)
    {
      ::TR::Core::SubjectQueryIdentity const& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        i.m_identity_value,
        v,
        sk);
    }

    // m_query_xml
    //
    {
      ::TR::Core::XmlQueryDocText const& v =
        o.m_query_xml;

      if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
            i.m_query_xml_value,
            v,
            sk))
        grew = true;
    }

    // m_uuid
    //
    {
      ::TR::Core::UUID const& v =
        o.m_uuid;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.m_uuid_value.capacity ());
      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_image (
        i.m_uuid_value,
        size,
        is_null,
        v);
      i.m_uuid_null = is_null;
      i.m_uuid_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_uuid_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_identity
    //
    {
      ::TR::Core::SubjectQueryIdentity& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        v,
        i.m_identity_value,
        db);
    }

    // m_query_xml
    //
    {
      ::TR::Core::XmlQueryDocText& v =
        o.m_query_xml;

      composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
        v,
        i.m_query_xml_value,
        db);
    }

    // m_uuid
    //
    {
      ::TR::Core::UUID& v =
        o.m_uuid;

      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_value (
        v,
        i.m_uuid_value,
        i.m_uuid_size,
        i.m_uuid_null);
    }
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    mysql::statement_kind sk (mysql::statement_select);
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        i.id_value,
        id,
        sk);
    }
  }

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::persist_statement[] =
  "INSERT INTO `SubjectQueryData` "
  "(`identity_subject_value1_value`, "
  "`identity_subject_value2_value`, "
  "`identity_query_joining_key_value`, "
  "`query_xml_value`, "
  "`uuid`) "
  "VALUES "
  "(?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::find_statement[] =
  "SELECT "
  "`SubjectQueryData`.`identity_subject_value1_value`, "
  "`SubjectQueryData`.`identity_subject_value2_value`, "
  "`SubjectQueryData`.`identity_query_joining_key_value`, "
  "`SubjectQueryData`.`query_xml_value`, "
  "`SubjectQueryData`.`uuid` "
  "FROM `SubjectQueryData` "
  "WHERE `SubjectQueryData`.`identity_subject_value1_value`=? AND `SubjectQueryData`.`identity_subject_value2_value`=? AND `SubjectQueryData`.`identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::update_statement[] =
  "UPDATE `SubjectQueryData` "
  "SET "
  "`query_xml_value`=?, "
  "`uuid`=? "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::erase_statement[] =
  "DELETE FROM `SubjectQueryData` "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::query_statement[] =
  "SELECT "
  "`SubjectQueryData`.`identity_subject_value1_value`, "
  "`SubjectQueryData`.`identity_subject_value2_value`, "
  "`SubjectQueryData`.`identity_query_joining_key_value`, "
  "`SubjectQueryData`.`query_xml_value`, "
  "`SubjectQueryData`.`uuid` "
  "FROM `SubjectQueryData`";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::erase_query_statement[] =
  "DELETE FROM `SubjectQueryData`";

  const char access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::table_name[] =
  "`SubjectQueryData`";

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.m_identity);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::pointer_type
  access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.m_identity);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::object_type >
  access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::TR::Core::SubjectQueryData, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // SubjectQueryView
  //

  bool access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_identity
    //
    if (composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::grow (
          i.m_identity_value, t + 0UL))
      grew = true;

    return grew;
  }

  void access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i)
  {
    using namespace mysql;

    mysql::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // m_identity
    //
    composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
      b + n, i.m_identity_value, sk);
    n += 3UL;
  }

  void access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_identity
    //
    {
      ::TR::Core::SubjectQueryIdentity& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        v,
        i.m_identity_value,
        db);
    }
  }

  access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::query_base_type
  access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "`SubjectQueryData`.`identity_subject_value1_value`, "
      "`SubjectQueryData`.`identity_subject_value2_value`, "
      "`SubjectQueryData`.`identity_query_joining_key_value` ");

    r += "FROM `SubjectQueryData`";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::view_type >
  access::view_traits_impl< ::TR::Core::SubjectQueryView, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) mysql::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }

  // SubjectJoinedQueryData
  //

  struct access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        id,
        i.m_identity_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_identity
    //
    if (composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::grow (
          i.m_identity_value, t + 0UL))
      grew = true;

    // m_query_xml
    //
    if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::grow (
          i.m_query_xml_value, t + 3UL))
      grew = true;

    // m_uuid
    //
    if (t[4UL])
    {
      i.m_uuid_value.capacity (i.m_uuid_size);
      grew = true;
    }

    // m_base_query_uuid
    //
    if (t[5UL])
    {
      i.m_base_query_uuid_value.capacity (i.m_base_query_uuid_size);
      grew = true;
    }

    // m_subject_query_uuid
    //
    if (t[6UL])
    {
      i.m_subject_query_uuid_value.capacity (i.m_subject_query_uuid_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // m_identity
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
        b + n, i.m_identity_value, sk);
      n += 3UL;
    }

    // m_query_xml
    //
    composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::bind (
      b + n, i.m_query_xml_value, sk);
    n += 1UL;

    // m_uuid
    //
    b[n].buffer_type = MYSQL_TYPE_BLOB;
    b[n].buffer = i.m_uuid_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_uuid_value.capacity ());
    b[n].length = &i.m_uuid_size;
    b[n].is_null = &i.m_uuid_null;
    n++;

    // m_base_query_uuid
    //
    b[n].buffer_type = MYSQL_TYPE_BLOB;
    b[n].buffer = i.m_base_query_uuid_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_base_query_uuid_value.capacity ());
    b[n].length = &i.m_base_query_uuid_size;
    b[n].is_null = &i.m_base_query_uuid_null;
    n++;

    // m_subject_query_uuid
    //
    b[n].buffer_type = MYSQL_TYPE_BLOB;
    b[n].buffer = i.m_subject_query_uuid_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_subject_query_uuid_value.capacity ());
    b[n].length = &i.m_subject_query_uuid_size;
    b[n].is_null = &i.m_subject_query_uuid_null;
    n++;
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    mysql::statement_kind sk (mysql::statement_select);
    composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_identity
    //
    if (sk == statement_insert)
    {
      ::TR::Core::SubjectQueryIdentity const& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        i.m_identity_value,
        v,
        sk);
    }

    // m_query_xml
    //
    {
      ::TR::Core::XmlQueryDocText const& v =
        o.m_query_xml;

      if (composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
            i.m_query_xml_value,
            v,
            sk))
        grew = true;
    }

    // m_uuid
    //
    {
      ::TR::Core::UUID const& v =
        o.m_uuid;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.m_uuid_value.capacity ());
      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_image (
        i.m_uuid_value,
        size,
        is_null,
        v);
      i.m_uuid_null = is_null;
      i.m_uuid_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_uuid_value.capacity ());
    }

    // m_base_query_uuid
    //
    {
      ::TR::Core::UUID const& v =
        o.m_base_query_uuid;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.m_base_query_uuid_value.capacity ());
      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_image (
        i.m_base_query_uuid_value,
        size,
        is_null,
        v);
      i.m_base_query_uuid_null = is_null;
      i.m_base_query_uuid_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_base_query_uuid_value.capacity ());
    }

    // m_subject_query_uuid
    //
    {
      ::TR::Core::UUID const& v =
        o.m_subject_query_uuid;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.m_subject_query_uuid_value.capacity ());
      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_image (
        i.m_subject_query_uuid_value,
        size,
        is_null,
        v);
      i.m_subject_query_uuid_null = is_null;
      i.m_subject_query_uuid_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_subject_query_uuid_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_identity
    //
    {
      ::TR::Core::SubjectQueryIdentity& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        v,
        i.m_identity_value,
        db);
    }

    // m_query_xml
    //
    {
      ::TR::Core::XmlQueryDocText& v =
        o.m_query_xml;

      composite_value_traits< ::TR::Core::XmlQueryDocText, id_mysql >::init (
        v,
        i.m_query_xml_value,
        db);
    }

    // m_uuid
    //
    {
      ::TR::Core::UUID& v =
        o.m_uuid;

      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_value (
        v,
        i.m_uuid_value,
        i.m_uuid_size,
        i.m_uuid_null);
    }

    // m_base_query_uuid
    //
    {
      ::TR::Core::UUID& v =
        o.m_base_query_uuid;

      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_value (
        v,
        i.m_base_query_uuid_value,
        i.m_base_query_uuid_size,
        i.m_base_query_uuid_null);
    }

    // m_subject_query_uuid
    //
    {
      ::TR::Core::UUID& v =
        o.m_subject_query_uuid;

      mysql::value_traits<
          ::TR::Core::UUID,
          mysql::id_blob >::set_value (
        v,
        i.m_subject_query_uuid_value,
        i.m_subject_query_uuid_size,
        i.m_subject_query_uuid_null);
    }
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    mysql::statement_kind sk (mysql::statement_select);
    {
      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        i.id_value,
        id,
        sk);
    }
  }

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::persist_statement[] =
  "INSERT INTO `SubjectJoinedQueryData` "
  "(`identity_subject_value1_value`, "
  "`identity_subject_value2_value`, "
  "`identity_query_joining_key_value`, "
  "`query_xml_value`, "
  "`uuid`, "
  "`base_query_uuid`, "
  "`subject_query_uuid`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::find_statement[] =
  "SELECT "
  "`SubjectJoinedQueryData`.`identity_subject_value1_value`, "
  "`SubjectJoinedQueryData`.`identity_subject_value2_value`, "
  "`SubjectJoinedQueryData`.`identity_query_joining_key_value`, "
  "`SubjectJoinedQueryData`.`query_xml_value`, "
  "`SubjectJoinedQueryData`.`uuid`, "
  "`SubjectJoinedQueryData`.`base_query_uuid`, "
  "`SubjectJoinedQueryData`.`subject_query_uuid` "
  "FROM `SubjectJoinedQueryData` "
  "WHERE `SubjectJoinedQueryData`.`identity_subject_value1_value`=? AND `SubjectJoinedQueryData`.`identity_subject_value2_value`=? AND `SubjectJoinedQueryData`.`identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::update_statement[] =
  "UPDATE `SubjectJoinedQueryData` "
  "SET "
  "`query_xml_value`=?, "
  "`uuid`=?, "
  "`base_query_uuid`=?, "
  "`subject_query_uuid`=? "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::erase_statement[] =
  "DELETE FROM `SubjectJoinedQueryData` "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_query_joining_key_value`=?";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::query_statement[] =
  "SELECT "
  "`SubjectJoinedQueryData`.`identity_subject_value1_value`, "
  "`SubjectJoinedQueryData`.`identity_subject_value2_value`, "
  "`SubjectJoinedQueryData`.`identity_query_joining_key_value`, "
  "`SubjectJoinedQueryData`.`query_xml_value`, "
  "`SubjectJoinedQueryData`.`uuid`, "
  "`SubjectJoinedQueryData`.`base_query_uuid`, "
  "`SubjectJoinedQueryData`.`subject_query_uuid` "
  "FROM `SubjectJoinedQueryData`";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::erase_query_statement[] =
  "DELETE FROM `SubjectJoinedQueryData`";

  const char access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::table_name[] =
  "`SubjectJoinedQueryData`";

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.m_identity);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::pointer_type
  access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.m_identity);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::object_type >
  access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::TR::Core::SubjectJoinedQueryData, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // SubjectJoinedQueryView
  //

  bool access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_identity
    //
    if (composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::grow (
          i.m_identity_value, t + 0UL))
      grew = true;

    return grew;
  }

  void access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i)
  {
    using namespace mysql;

    mysql::statement_kind sk (statement_select);
    ODB_POTENTIALLY_UNUSED (sk);

    std::size_t n (0);

    // m_identity
    //
    composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::bind (
      b + n, i.m_identity_value, sk);
    n += 3UL;
  }

  void access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::
  init (view_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_identity
    //
    {
      ::TR::Core::SubjectQueryIdentity& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::SubjectQueryIdentity, id_mysql >::init (
        v,
        i.m_identity_value,
        db);
    }
  }

  access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::query_base_type
  access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::
  query_statement (const query_base_type& q)
  {
    query_base_type r (
      "SELECT "
      "`SubjectJoinedQueryData`.`identity_subject_value1_value`, "
      "`SubjectJoinedQueryData`.`identity_subject_value2_value`, "
      "`SubjectJoinedQueryData`.`identity_query_joining_key_value` ");

    r += "FROM `SubjectJoinedQueryData`";

    if (!q.empty ())
    {
      r += " ";
      r += q.clause_prefix ();
      r += q;
    }

    return r;
  }

  result< access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::view_type >
  access::view_traits_impl< ::TR::Core::SubjectJoinedQueryView, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_view<view_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.image_binding ());

    if (im.version != sts.image_version () || imb.version == 0)
    {
      bind (imb.bind, im);
      sts.image_version (im.version);
      imb.version++;
    }

    const query_base_type& qs (query_statement (q));
    qs.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        qs.clause (),
        false,
        true,
        qs.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::view_result_impl<view_type> > r (
      new (shared) mysql::view_result_impl<view_type> (
        qs, st, sts, 0));

    return result<view_type> (r);
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS `SubjectJoinedQueryData`");
          db.execute ("DROP TABLE IF EXISTS `SubjectQueryData`");
          db.execute ("DROP TABLE IF EXISTS `QueryJoiningData`");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE `QueryJoiningData` (\n"
                      "  `key` INT NOT NULL PRIMARY KEY AUTO_INCREMENT,\n"
                      "  `input_uuid_value` BINARY(16) NULL,\n"
                      "  `output_uuid_value` BINARY(16) NULL,\n"
                      "  `default_query_xml_value` TEXT NULL)\n"
                      " ENGINE=InnoDB");
          db.execute ("CREATE TABLE `SubjectQueryData` (\n"
                      "  `identity_subject_value1_value` INT NOT NULL,\n"
                      "  `identity_subject_value2_value` INT NOT NULL,\n"
                      "  `identity_query_joining_key_value` INT NOT NULL,\n"
                      "  `query_xml_value` TEXT NOT NULL,\n"
                      "  `uuid` BINARY(16) NULL,\n"
                      "  PRIMARY KEY (`identity_subject_value1_value`,\n"
                      "               `identity_subject_value2_value`,\n"
                      "               `identity_query_joining_key_value`))\n"
                      " ENGINE=InnoDB");
          db.execute ("CREATE TABLE `SubjectJoinedQueryData` (\n"
                      "  `identity_subject_value1_value` INT NOT NULL,\n"
                      "  `identity_subject_value2_value` INT NOT NULL,\n"
                      "  `identity_query_joining_key_value` INT NOT NULL,\n"
                      "  `query_xml_value` TEXT NOT NULL,\n"
                      "  `uuid` BINARY(16) NULL,\n"
                      "  `base_query_uuid` BINARY(16) NULL,\n"
                      "  `subject_query_uuid` BINARY(16) NULL,\n"
                      "  PRIMARY KEY (`identity_subject_value1_value`,\n"
                      "               `identity_subject_value2_value`,\n"
                      "               `identity_query_joining_key_value`))\n"
                      " ENGINE=InnoDB");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_mysql,
    "",
    &create_schema);
}

#include <odb/post.hxx>
