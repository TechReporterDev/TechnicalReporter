// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "ScheduleData-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // ScheduleData
  //

  struct access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  id (const id_image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        id,
        i.m_key_value,
        i.m_key_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_key
    //
    t[0UL] = 0;

    // m_name
    //
    if (t[1UL])
    {
      i.m_name_value.capacity (i.m_name_size);
      grew = true;
    }

    // m_period
    //
    t[2UL] = 0;

    // m_day
    //
    t[3UL] = 0;

    // m_week_day
    //
    t[4UL] = 0;

    // m_day_time
    //
    t[5UL] = 0;

    // m_scheduled_time
    //
    t[6UL] = 0;

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // m_key
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONG;
      b[n].is_unsigned = 0;
      b[n].buffer = &i.m_key_value;
      b[n].is_null = &i.m_key_null;
      n++;
    }

    // m_name
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.m_name_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_name_value.capacity ());
    b[n].length = &i.m_name_size;
    b[n].is_null = &i.m_name_null;
    n++;

    // m_period
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_period_value;
    b[n].is_null = &i.m_period_null;
    n++;

    // m_day
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_day_value;
    b[n].is_null = &i.m_day_null;
    n++;

    // m_week_day
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_week_day_value;
    b[n].is_null = &i.m_week_day_null;
    n++;

    // m_day_time
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_day_time_value;
    b[n].is_null = &i.m_day_time_null;
    n++;

    // m_scheduled_time
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_scheduled_time_value;
    b[n].is_null = &i.m_scheduled_time_null;
    n++;
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_key
    //
    if (sk == statement_insert)
    {
      ::TR::Core::Key const& v =
        o.m_key;

      bool is_null (false);
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_image (
        i.m_key_value, is_null, v);
      i.m_key_null = is_null;
    }

    // m_name
    //
    {
      ::std::string const& v =
        o.m_name;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.m_name_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.m_name_value,
        size,
        is_null,
        v);
      i.m_name_null = is_null;
      i.m_name_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_name_value.capacity ());
    }

    // m_period
    //
    {
      int const& v =
        o.m_period;

      bool is_null (false);
      mysql::value_traits<
          int,
          mysql::id_long >::set_image (
        i.m_period_value, is_null, v);
      i.m_period_null = is_null;
    }

    // m_day
    //
    {
      int const& v =
        o.m_day;

      bool is_null (false);
      mysql::value_traits<
          int,
          mysql::id_long >::set_image (
        i.m_day_value, is_null, v);
      i.m_day_null = is_null;
    }

    // m_week_day
    //
    {
      int const& v =
        o.m_week_day;

      bool is_null (false);
      mysql::value_traits<
          int,
          mysql::id_long >::set_image (
        i.m_week_day_value, is_null, v);
      i.m_week_day_null = is_null;
    }

    // m_day_time
    //
    {
      ::time_t const& v =
        o.m_day_time;

      bool is_null (false);
      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_image (
        i.m_day_time_value, is_null, v);
      i.m_day_time_null = is_null;
    }

    // m_scheduled_time
    //
    {
      ::time_t const& v =
        o.m_scheduled_time;

      bool is_null (false);
      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_image (
        i.m_scheduled_time_value, is_null, v);
      i.m_scheduled_time_null = is_null;
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_key
    //
    {
      ::TR::Core::Key& v =
        o.m_key;

      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_value (
        v,
        i.m_key_value,
        i.m_key_null);
    }

    // m_name
    //
    {
      ::std::string& v =
        o.m_name;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.m_name_value,
        i.m_name_size,
        i.m_name_null);
    }

    // m_period
    //
    {
      int& v =
        o.m_period;

      mysql::value_traits<
          int,
          mysql::id_long >::set_value (
        v,
        i.m_period_value,
        i.m_period_null);
    }

    // m_day
    //
    {
      int& v =
        o.m_day;

      mysql::value_traits<
          int,
          mysql::id_long >::set_value (
        v,
        i.m_day_value,
        i.m_day_null);
    }

    // m_week_day
    //
    {
      int& v =
        o.m_week_day;

      mysql::value_traits<
          int,
          mysql::id_long >::set_value (
        v,
        i.m_week_day_value,
        i.m_week_day_null);
    }

    // m_day_time
    //
    {
      ::time_t& v =
        o.m_day_time;

      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_value (
        v,
        i.m_day_time_value,
        i.m_day_time_null);
    }

    // m_scheduled_time
    //
    {
      ::time_t& v =
        o.m_scheduled_time;

      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_value (
        v,
        i.m_scheduled_time_value,
        i.m_scheduled_time_null);
    }
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          ::TR::Core::Key,
          mysql::id_long >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::persist_statement[] =
  "INSERT INTO `ScheduleData` "
  "(`key`, "
  "`name`, "
  "`period`, "
  "`day`, "
  "`week_day`, "
  "`day_time`, "
  "`scheduled_time`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::find_statement[] =
  "SELECT "
  "`ScheduleData`.`key`, "
  "`ScheduleData`.`name`, "
  "`ScheduleData`.`period`, "
  "`ScheduleData`.`day`, "
  "`ScheduleData`.`week_day`, "
  "`ScheduleData`.`day_time`, "
  "`ScheduleData`.`scheduled_time` "
  "FROM `ScheduleData` "
  "WHERE `ScheduleData`.`key`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::update_statement[] =
  "UPDATE `ScheduleData` "
  "SET "
  "`name`=?, "
  "`period`=?, "
  "`day`=?, "
  "`week_day`=?, "
  "`day_time`=?, "
  "`scheduled_time`=? "
  "WHERE `key`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::erase_statement[] =
  "DELETE FROM `ScheduleData` "
  "WHERE `key`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::query_statement[] =
  "SELECT "
  "`ScheduleData`.`key`, "
  "`ScheduleData`.`name`, "
  "`ScheduleData`.`period`, "
  "`ScheduleData`.`day`, "
  "`ScheduleData`.`week_day`, "
  "`ScheduleData`.`day_time`, "
  "`ScheduleData`.`scheduled_time` "
  "FROM `ScheduleData`";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::erase_query_statement[] =
  "DELETE FROM `ScheduleData`";

  const char access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::table_name[] =
  "`ScheduleData`";

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im.m_key_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.m_key = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.m_key);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::pointer_type
  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.m_key);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::object_type >
  access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::TR::Core::ScheduleData, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }

  // ScheduledTaskIdentity
  //

  bool access::composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_subject
    //
    if (composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::grow (
          i.m_subject_value, t + 0UL))
      grew = true;

    // m_operation
    //
    if (composite_value_traits< ::TR::Core::PeriodicOperationVariant, id_mysql >::grow (
          i.m_operation_value, t + 2UL))
      grew = true;

    return grew;
  }

  void access::composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // m_subject
    //
    composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::bind (
      b + n, i.m_subject_value, sk);
    n += 2UL;

    // m_operation
    //
    composite_value_traits< ::TR::Core::PeriodicOperationVariant, id_mysql >::bind (
      b + n, i.m_operation_value, sk);
    n += 2UL;
  }

  bool access::composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::
  init (image_type& i,
        const value_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_subject
    //
    {
      ::TR::Core::SubjectIdentity const& v =
        o.m_subject;

      composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::init (
        i.m_subject_value,
        v,
        sk);
    }

    // m_operation
    //
    {
      ::TR::Core::PeriodicOperationVariant const& v =
        o.m_operation;

      if (composite_value_traits< ::TR::Core::PeriodicOperationVariant, id_mysql >::init (
            i.m_operation_value,
            v,
            sk))
        grew = true;
    }

    return grew;
  }

  void access::composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_subject
    //
    {
      ::TR::Core::SubjectIdentity& v =
        o.m_subject;

      composite_value_traits< ::TR::Core::SubjectIdentity, id_mysql >::init (
        v,
        i.m_subject_value,
        db);
    }

    // m_operation
    //
    {
      ::TR::Core::PeriodicOperationVariant& v =
        o.m_operation;

      composite_value_traits< ::TR::Core::PeriodicOperationVariant, id_mysql >::init (
        v,
        i.m_operation_value,
        db);
    }
  }

  // ScheduledTaskData
  //

  struct access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      mysql::connection&,
      image_type&,
      id_image_type&,
      mysql::binding&,
      mysql::binding&)
    {
    }
  };

  access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::id_type
  access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::init (
        id,
        i.m_identity_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  grow (image_type& i,
        my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // m_identity
    //
    if (composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::grow (
          i.m_identity_value, t + 0UL))
      grew = true;

    // m_schedule_key
    //
    t[4UL] = 0;

    // m_launch_time
    //
    t[5UL] = 0;

    // m_result
    //
    t[6UL] = 0;

    // m_message
    //
    if (t[7UL])
    {
      i.m_message_value.capacity (i.m_message_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // m_identity
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::bind (
        b + n, i.m_identity_value, sk);
      n += 4UL;
    }

    // m_schedule_key
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_schedule_key_value;
    b[n].is_null = &i.m_schedule_key_null;
    n++;

    // m_launch_time
    //
    b[n].buffer_type = MYSQL_TYPE_LONGLONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_launch_time_value;
    b[n].is_null = &i.m_launch_time_null;
    n++;

    // m_result
    //
    b[n].buffer_type = MYSQL_TYPE_TINY;
    b[n].is_unsigned = 0;
    b[n].buffer = &i.m_result_value;
    b[n].is_null = &i.m_result_null;
    n++;

    // m_message
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i.m_message_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i.m_message_value.capacity ());
    b[n].length = &i.m_message_size;
    b[n].is_null = &i.m_message_null;
    n++;
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    mysql::statement_kind sk (mysql::statement_select);
    composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  init (image_type& i,
        const object_type& o,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // m_identity
    //
    if (sk == statement_insert)
    {
      ::TR::Core::ScheduledTaskIdentity const& v =
        o.m_identity;

      if (composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::init (
            i.m_identity_value,
            v,
            sk))
        grew = true;
    }

    // m_schedule_key
    //
    {
      ::boost::optional< int > const& v =
        o.m_schedule_key;

      bool is_null (true);
      mysql::value_traits<
          ::boost::optional< int >,
          mysql::id_long >::set_image (
        i.m_schedule_key_value, is_null, v);
      i.m_schedule_key_null = is_null;
    }

    // m_launch_time
    //
    {
      ::time_t const& v =
        o.m_launch_time;

      bool is_null (false);
      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_image (
        i.m_launch_time_value, is_null, v);
      i.m_launch_time_null = is_null;
    }

    // m_result
    //
    {
      bool const& v =
        o.m_result;

      bool is_null (false);
      mysql::value_traits<
          bool,
          mysql::id_tiny >::set_image (
        i.m_result_value, is_null, v);
      i.m_result_null = is_null;
    }

    // m_message
    //
    {
      ::std::string const& v =
        o.m_message;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i.m_message_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i.m_message_value,
        size,
        is_null,
        v);
      i.m_message_null = is_null;
      i.m_message_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i.m_message_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // m_identity
    //
    {
      ::TR::Core::ScheduledTaskIdentity& v =
        o.m_identity;

      composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::init (
        v,
        i.m_identity_value,
        db);
    }

    // m_schedule_key
    //
    {
      ::boost::optional< int >& v =
        o.m_schedule_key;

      mysql::value_traits<
          ::boost::optional< int >,
          mysql::id_long >::set_value (
        v,
        i.m_schedule_key_value,
        i.m_schedule_key_null);
    }

    // m_launch_time
    //
    {
      ::time_t& v =
        o.m_launch_time;

      mysql::value_traits<
          ::time_t,
          mysql::id_longlong >::set_value (
        v,
        i.m_launch_time_value,
        i.m_launch_time_null);
    }

    // m_result
    //
    {
      bool& v =
        o.m_result;

      mysql::value_traits<
          bool,
          mysql::id_tiny >::set_value (
        v,
        i.m_result_value,
        i.m_result_null);
    }

    // m_message
    //
    {
      ::std::string& v =
        o.m_message;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i.m_message_value,
        i.m_message_size,
        i.m_message_null);
    }
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    bool grew (false);
    mysql::statement_kind sk (mysql::statement_select);
    {
      if (composite_value_traits< ::TR::Core::ScheduledTaskIdentity, id_mysql >::init (
            i.id_value,
            id,
            sk))
        grew = true;
    }

    if (grew)
      i.version++;
  }

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::persist_statement[] =
  "INSERT INTO `ScheduledTaskData` "
  "(`identity_subject_value1_value`, "
  "`identity_subject_value2_value`, "
  "`identity_operation_value1_report_type_uuid_value`, "
  "`identity_operation_value2_action_shortcut_uuid`, "
  "`schedule_key`, "
  "`launch_time`, "
  "`result`, "
  "`message`) "
  "VALUES "
  "(?, ?, ?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::find_statement[] =
  "SELECT "
  "`ScheduledTaskData`.`identity_subject_value1_value`, "
  "`ScheduledTaskData`.`identity_subject_value2_value`, "
  "`ScheduledTaskData`.`identity_operation_value1_report_type_uuid_value`, "
  "`ScheduledTaskData`.`identity_operation_value2_action_shortcut_uuid`, "
  "`ScheduledTaskData`.`schedule_key`, "
  "`ScheduledTaskData`.`launch_time`, "
  "`ScheduledTaskData`.`result`, "
  "`ScheduledTaskData`.`message` "
  "FROM `ScheduledTaskData` "
  "WHERE `ScheduledTaskData`.`identity_subject_value1_value`=? AND `ScheduledTaskData`.`identity_subject_value2_value`=? AND `ScheduledTaskData`.`identity_operation_value1_report_type_uuid_value`=? AND `ScheduledTaskData`.`identity_operation_value2_action_shortcut_uuid`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::update_statement[] =
  "UPDATE `ScheduledTaskData` "
  "SET "
  "`schedule_key`=?, "
  "`launch_time`=?, "
  "`result`=?, "
  "`message`=? "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_operation_value1_report_type_uuid_value`=? AND `identity_operation_value2_action_shortcut_uuid`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::erase_statement[] =
  "DELETE FROM `ScheduledTaskData` "
  "WHERE `identity_subject_value1_value`=? AND `identity_subject_value2_value`=? AND `identity_operation_value1_report_type_uuid_value`=? AND `identity_operation_value2_action_shortcut_uuid`=?";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::query_statement[] =
  "SELECT "
  "`ScheduledTaskData`.`identity_subject_value1_value`, "
  "`ScheduledTaskData`.`identity_subject_value2_value`, "
  "`ScheduledTaskData`.`identity_operation_value1_report_type_uuid_value`, "
  "`ScheduledTaskData`.`identity_operation_value2_action_shortcut_uuid`, "
  "`ScheduledTaskData`.`schedule_key`, "
  "`ScheduledTaskData`.`launch_time`, "
  "`ScheduledTaskData`.`result`, "
  "`ScheduledTaskData`.`message` "
  "FROM `ScheduledTaskData`";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::erase_query_statement[] =
  "DELETE FROM `ScheduledTaskData`";

  const char access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::table_name[] =
  "`ScheduledTaskData`";

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;
    using mysql::update_statement;

    callback (db, obj, callback_event::pre_update);

    mysql::transaction& tr (mysql::transaction::current ());
    mysql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.m_identity);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::pointer_type
  access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.m_identity);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::object_type >
  access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        false,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::TR::Core::ScheduledTaskData, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS `ScheduledTaskData`");
          db.execute ("DROP TABLE IF EXISTS `ScheduleData`");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE `ScheduleData` (\n"
                      "  `key` INT NOT NULL PRIMARY KEY AUTO_INCREMENT,\n"
                      "  `name` TEXT NOT NULL,\n"
                      "  `period` INT NOT NULL,\n"
                      "  `day` INT NOT NULL,\n"
                      "  `week_day` INT NOT NULL,\n"
                      "  `day_time` BIGINT NOT NULL,\n"
                      "  `scheduled_time` BIGINT NOT NULL)\n"
                      " ENGINE=InnoDB");
          db.execute ("CREATE TABLE `ScheduledTaskData` (\n"
                      "  `identity_subject_value1_value` INT NOT NULL,\n"
                      "  `identity_subject_value2_value` INT NOT NULL,\n"
                      "  `identity_operation_value1_report_type_uuid_value` BINARY(16) NOT NULL,\n"
                      "  `identity_operation_value2_action_shortcut_uuid` BINARY(16) NOT NULL,\n"
                      "  `schedule_key` INT NULL,\n"
                      "  `launch_time` BIGINT NOT NULL,\n"
                      "  `result` TINYINT(1) NOT NULL,\n"
                      "  `message` TEXT NOT NULL,\n"
                      "  PRIMARY KEY (`identity_subject_value1_value`,\n"
                      "               `identity_subject_value2_value`,\n"
                      "               `identity_operation_value1_report_type_uuid_value`,\n"
                      "               `identity_operation_value2_action_shortcut_uuid`))\n"
                      " ENGINE=InnoDB");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_mysql,
    "",
    &create_schema);
}

#include <odb/post.hxx>
